<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="prodcastd.prodcast.samayu.com.prodcastd">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />



    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/distribution_icon"
        android:roundIcon="@drawable/p_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home" />
        <activity android:name=".customer_activity" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CustomerActivity">


        </activity>
        <activity android:name=".CustomerPasswordActivity"></activity>
        <activity android:name=".OrderEntry"></activity>
        <activity android:name=".OutstandingbillsPayment"></activity>
    </application>

</manifest>